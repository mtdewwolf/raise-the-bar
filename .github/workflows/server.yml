name: Server tests

on:
  push:
    paths: ['index.html', 'server/**', '.github/workflows/server.yml']
  pull_request:
    paths: ['index.html', 'server/**', '.github/workflows/server.yml']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm test
        working-directory: server
